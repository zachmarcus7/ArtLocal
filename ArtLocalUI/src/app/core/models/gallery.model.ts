export interface Gallery {
    galleryId: string;
    name: string;
    address: string;
    city: string;
    state: string;
    postalCode: number;
    country: string;
    phoneNumber: string;
}